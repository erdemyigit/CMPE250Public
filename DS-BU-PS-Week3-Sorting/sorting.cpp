#include <iostream>
#include "sorting.h"

using namespace std;

template<typename T>
void insertionSort(T *a, int n) {
}

template<typename T>
void mergeSort(T *a, int n) {
}

template<typename T>
void quicksort(T *a, int n) {
}
