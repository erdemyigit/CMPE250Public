using namespace std;

template<typename T>
void insertionSort(T *a, int n);

template<typename T>
void mergeSort(T *a, int n);

template<typename T>
void quickSort(T *a, int n);

#include "sorting.cpp"
